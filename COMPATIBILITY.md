# Совместимость расширения

## ✅ Поддерживаемые версии Jira

### Jira Cloud (Atlassian Cloud)
- ✅ **Полная поддержка**
- ✅ Все функции работают
- ✅ Автоматическое определение проекта
- ✅ Заполнение форм работает

### Jira Server
- ✅ **Полная поддержка**
- ✅ Все функции работают
- ✅ Поддержка HTTP и HTTPS
- ✅ Классический интерфейс

### Jira Data Center
- ✅ **Полная поддержка**
- ✅ Все функции работают
- ✅ Поддержка кластерных установок

---

## ✅ Поддерживаемые типы проектов

### Classic Projects
- ✅ **Полная поддержка**
- ✅ Все селекторы работают
- ✅ Определение project key
- ✅ Заполнение форм

### Next-Gen Projects
- ✅ **Полная поддержка**
- ✅ Современные селекторы
- ✅ Поддержка новых полей
- ✅ ContentEditable поля

### Team-Managed Projects
- ✅ **Полная поддержка**
- ✅ Адаптированные селекторы
- ✅ Определение проекта

---

## ✅ Универсальные селекторы

### Определение полей формы

#### Title/Summary поле:
- `#summary` (ID)
- `[name="summary"]` (name attribute)
- `[data-testid="summary"]` (test ID)
- `input[aria-label*="Summary"]` (aria-label)
- `input[placeholder*="summary"]` (placeholder)
- `.summary-field input` (class)
- `.issue-summary` (class)
- `[data-field-id="summary"]` (data attribute)

#### Description поле:
- `#description` (ID)
- `[name="description"]` (name attribute)
- `[data-testid="description"]` (test ID)
- `textarea[aria-label*="Description"]` (aria-label)
- `.ql-editor` (Quill editor)
- `[contenteditable="true"]` (contenteditable)
- `.ak-editor-content-area` (Atlassian editor)
- `[role="textbox"]` (role attribute)

#### Labels поле:
- `#labels` (ID)
- `[name="labels"]` (name attribute)
- `[data-testid="labels"]` (test ID)
- `input[aria-label*="Labels"]` (aria-label)
- `.labels-field input` (class)

#### Priority поле:
- `#priority` (ID)
- `[name="priority"]` (name attribute)
- `[data-testid="priority"]` (test ID)
- `select[aria-label*="Priority"]` (aria-label)
- `.priority-field select` (class)

---

## ✅ Определение Project Key

### Методы определения (в порядке приоритета):

1. **Из URL:**
   - `projectKey=XXX`
   - `/browse/XXX`
   - `/projects/XXX`
   - `/jira/software/projects/XXX`
   - `/browse/XXX-123`

2. **Из DOM элементов:**
   - `[data-project-key]`
   - `[data-testid="project-key"]`
   - `.project-key`
   - `meta[name="ajs-project-key"]`

3. **Из issue ключей:**
   - `[data-issue-key]`
   - `a[href*="/browse/"]`
   - Текст элементов

---

## ✅ Поддерживаемые URL паттерны

- `https://*.atlassian.net/*` (Cloud)
- `https://*.atlassian.com/*` (Cloud альтернативный)
- `https://*.jira.com/*` (Server/Data Center)
- `http://*.jira.com/*` (Server без SSL)

---

## ✅ Типы полей форм

### Input поля:
- ✅ Стандартные `<input>`
- ✅ `<textarea>`
- ✅ ContentEditable divs
- ✅ Quill Editor (`.ql-editor`)
- ✅ Atlassian Editor (`.ak-editor-content-area`)

### Select поля:
- ✅ Стандартные `<select>`
- ✅ Кастомные селекты
- ✅ Поиск по тексту опций

---

## ⚠️ Ограничения

1. **Требуется страница создания задачи** для автозаполнения
2. **Backend требуется** только для полного анализа проекта
3. **OpenAI ключ обязателен** для генерации задач

---

## 🔧 Адаптация под новые версии

Расширение использует:
- ✅ Множественные селекторы (fallback)
- ✅ Универсальные паттерны
- ✅ Проверка видимости элементов
- ✅ Graceful degradation

Если Jira обновит интерфейс, расширение продолжит работать благодаря множественным селекторам.

---

**Версия:** 1.0.0  
**Последнее обновление:** 2024

